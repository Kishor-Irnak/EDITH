/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/EDITH",
  assetPrefix: "/EDITH",
};

export default nextConfig;
